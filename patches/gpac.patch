diff --git a/Makefile b/Makefile
index 820c49b..34f957d 100644
--- a/Makefile
+++ b/Makefile
@@ -218,7 +218,9 @@ ifeq ($(GPAC_ENST), yes)
 endif
 	mkdir -p "$(DESTDIR)$(prefix)/$(libdir)"
 	$(INSTALL) $(INSTFLAGS) -m 644 "./bin/gcc/libgpac_static.a" "$(DESTDIR)$(prefix)/$(libdir)"
+ifneq ($(MP4BOX_STATIC), yes)
 	$(MAKE) installdylib
+endif
 
 uninstall-lib:
 	rm -rf "$(prefix)/include/gpac/internal"
diff --git a/configure b/configure
index fb1a4d7..3776feb 100755
--- a/configure
+++ b/configure
@@ -580,6 +580,7 @@ EOF
         mingw32="yes"
         win32="yes"
         extralibs="$extralibs -lws2_32 -lwinmm"
+        want_pic="no"
         CFLAGS="$CFLAGS -D_LARGEFILE_SOURCE -D_FILE_OFFSET_BITS=64"
         if test "$cross_prefix" != "" ; then
             GPAC_SH_FLAGS=""
